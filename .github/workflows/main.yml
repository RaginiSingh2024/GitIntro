name: Echo Hello on Push to Main

on:
  push:
    branches:
      - main

jobs:
  echo_hello:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Echo Hello
      run: echo "Hello"
